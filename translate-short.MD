# 쇼츠 변환 작업 규칙 (translate-short)

다운로드한 유튜브 쇼츠를 **해숏티 / 랭킹굳** 채널용으로 재브랜딩하고, 자막을 재작성해
**원본과 중복되지 않게** 만들어 재업로드하기 위한 파이프라인 규칙 문서.

원본 영상은 이미 1080×1920(9:16)로 헤더·자막이 픽셀에 합성된 완성본이라, "레이어를 다시 편집"하는 게
아니라 **특정 영역을 덮어 새로 그리고(overlay) / 오디오를 교체**하는 방식으로 처리한다.

---

## 0. 폴더 규칙

| 폴더 | 용도 |
|---|---|
| `original_shorts/` | 자막형(해숏티) **입력** 원본 |
| `output_shorts/` | 자막형 **결과** (해숏티 브랜딩 완성본) |
| `ranking_original/` | 랭킹형(랭킹굳) **입력** 원본 |
| `ranking_shorts/` | 랭킹형 **결과** (랭킹굳 브랜딩 완성본) |
| `ranking_shuffled/` | 랭킹형 **셔플 결과** (`--shuffle` 사용 시 여기로 저장) |
| `.shorts_work/` | 파이프라인 스크립트·자산·중간데이터 (git: 스크립트/폰트/로고만 추적, 데이터·로그·바이너리 제외) |

> 결과 영상(`*.mp4`)은 모두 git 무시. 자동 유튜브 업로드(launchd)는 **`output_shorts/`만** 감시한다.

## 공통 자산 (`.shorts_work/`)

- `BlackHanSans.ttf` — 제목/자막 폰트 (굵은 고딕)
- `logo_circle.png` — 해숏티 원형 로고
- `rank_logo_circle.png` — 랭킹굳 원형 로고
- `ocr` / `ocr.swift` — Apple Vision 기반 한글 OCR (컴파일: `swiftc -O ocr.swift -o ocr`)

## 공통 도구 / 의존성

- **ffmpeg** (합성·오디오), **yt-dlp** (다운로드)
- Python: `numpy`, `scipy`, `Pillow(PIL)`, `torch`+`torchaudio`(2.9.1 페어), `demucs`, `denoiser`
- macOS **Apple Vision**(Swift) OCR, **MPS**(GPU) 가속

---

## 파이프라인 A — 자막형 (해숏티)

### 대상 포맷
상단 **흰색 헤더**(채널로고 + 제목) + 가운데 **영상** + 하단 **흰색 자막 밴드**(장면마다 바뀌는 내레이션 자막).
예: `오늘의 미 / today beauty` 류.

### 처리 단계
1. **경계 자동 감지** (`prep.py` + `detect2.py`)
   - 여러 프레임의 **시간적 변화(temporal variance)**로 "움직이는 영상 밴드"를 찾음
     (헤더·자막은 정지 → 변화 작음 / 영상 → 변화 큼). 가장 큰 연속 움직임 구간 = 영상.
   - `headerBottom` = 영상 시작 y, `footerTop` = 영상 끝 y+1.
   - 감지 실패 시 표준값(예: 490)으로 폴백.
2. **자막 OCR** (`prep.py`)
   - 하단 자막 영역(`footerTop`~)을 2fps로 크롭 → Apple Vision 한글 OCR → 연속 동일 자막을 묶어
     `{start, end, text}` 세그먼트로 추출.
3. **제목 추출** — 파일명에서 날짜접두(`YYYYMMDD_`)와 `[영상ID]`를 제거한 나머지.
4. **자막 패러프레이즈** (멀티 에이전트 / LLM) — 각 세그먼트 `text` → `new` 필드로 재작성.
5. **렌더** (`render.py`) → `output_shorts/`.
6. **(선택) 배경음악 제거** (`voice_swap.py`) — 오디오만 교체.

### 변환 규칙 (무엇을 바꾸고 / 유지)
- **상단 헤더 교체**: `0 ~ headerBottom`을 흰색으로 덮고 → **해숏티 로고 + "해숏티" + 제목**을 다시 그림.
  - 제목 기본 폰트 **80px**(길면 자동 축소), 가운데 정렬, 검정.
- **하단 자막 교체**: `footerTop ~ 하단`을 흰색으로 덮고 → **패러프레이즈된 새 자막**을 원래 타임코드대로
  다시 그림. 세그먼트 사이 틈은 다음 세그먼트 시작까지 연장(깜빡임 방지).
  - 자막 폰트 최대 2줄에 맞게 자동 축소, 가운데, 검정.
- **영상 좌우반전(hflip)**: 가운데 영상만 반전. **헤더·자막 텍스트는 정상 방향 유지**
  (텍스트는 반전 후 그 위에 그리므로 뒤집히지 않음).
- **오디오**: 기본은 원본 유지(무손실 복사). 배경음악 제거 옵션 사용 시에만 교체.
- 출력: H.264 + AAC, `+faststart`. **파일명은 `<제목>.mp4`** (아래 [파일명 규칙](#파일명-규칙) 참조).

### 패러프레이즈 규칙
- **뜻/내용 동일** (정보 추가·삭제 금지).
- **어순(語順) + 어휘 변경** — 원문과 글자가 최대한 겹치지 않게.
- 길이 유사(원문 대비 약 ±20%).
- 고유명사·숫자·날짜·단위(예: KTX, 6.25, 2019년 6월 12일, 이국종)는 정확히 유지하되 문장은 재구성.
- 세그먼트 1:1 대응 — 병합·분할·순서변경·추가·삭제 금지.

### 배경음악 제거 규칙 (`voice_swap.py`)
- 목적: **목소리(내레이션)만 남기고 배경음악(그 안의 노래 보컬 포함)을 억제**.
- 방식: 전경 말소리를 잡는 **음성강화 모델(denoiser dns64)** → 배경(음악+배경 노래)을 눌러줌.
  - 16kHz로 처리되어 음질이 약간 손실됨(전화기 느낌 가능). 대신 배경 억제는 더 강함.
- 참고 한계:
  - **악기 BGM** → `Demucs`(음원분리)로 완벽 제거 가능(보컬 스템만 추출).
  - **노래 BGM** → 말하는 목소리와 노래 목소리 둘 다 "사람 목소리"라 완벽 분리는 불가.
    이 경우 denoiser 방식이 그나마 배경 노래를 더 눌러준다.
- 처리: 영상 스트림은 복사, 오디오만 교체(in-place). 16000Hz면 적용된 것.

---

## 파이프라인 B — 랭킹형 (랭킹굳)

### 대상 포맷
상단 **검정 헤더**(2색 제목: 노랑 "설명" + 흰 "랭킹 TOPn") + **풀화면 영상**
(좌측 랭킹 리스트 `1.2.3…`, 하단 자막 박스가 영상에 베이크됨). 예: `역대급 … 랭킹 TOP5` 류.

### 처리 단계 (`rank_pipeline.py`)
1. **검정 헤더 자동 감지** — 시간적 변화 기반(A와 동일). 감지 실패 시 표준값(490) 폴백.
2. **제목 OCR** — 헤더 영역(`0~headerBottom`)을 Apple Vision OCR.
3. **제목 분리** — "랭킹"(없으면 "TOP") 기준으로 `설명(노랑)` + `랭킹 TOPn(흰)`으로 나눔.
4. **해숏티/랭킹굳 스타일 헤더 재생성 + 하단 배너** → 렌더 → `ranking_shorts/`.

### 변환 규칙 (무엇을 바꾸고 / 유지)
- **상단 헤더 교체**: 검정 배경으로 덮고 → 제목만 다시 그림(**로고·워드마크 없음**).
  - 설명줄 = **노랑(#ffd400)**, "랭킹 TOPn" = **흰색**, 굵게, 가운데, 제목은 헤더 하단쪽에 배치.
  - 폰트 자동 축소(폭에 맞게).
- **하단 배너 추가**: 맨 아래 ~160px에 **검정 + 노랑 라인 + 랭킹굳 로고 + "랭킹굳"** → 원본 하단 일부를 가림(차별화).
- **좌우반전 없음** — 영상에 베이크된 랭킹 숫자·자막·제목이 뒤집혀 깨지므로 반전하지 않음.
- **자막/랭킹/영상/오디오 그대로 유지** (제목 브랜딩과 하단 배너만 변경).
- 출력: H.264 + AAC, `+faststart`. **파일명은 `<제목>.mp4`** (아래 [파일명 규칙](#파일명-규칙) 참조).

### (선택) 셔플 — `--shuffle`
- **랭킹 항목(클립) 순서를 무작위로 섞는다.**
  1. 헤더 OCR("랭킹 TOP**N**")에서 **N**(항목 수)을 얻는다.
  2. 장면컷(scene detection)으로 자른 뒤, **정확히 N개 클립**이 되도록 조정(많으면 짧은 것 병합, 적으면 긴 것 분할).
  3. N개 클립의 **순서를 무작위 재배치**해 재결합(오디오도 함께).
- 각 클립은 **자기 랭킹 번호·자막을 그대로** 달고 다른 순서로 재생됨 → "랭킹을 섞은" 결과.
- 헤더/제목은 **원본에서 감지·OCR**한 뒤 셔플된 영상 위에 다시 그림(상단 검정 헤더는 정지라 동일).
- ⚠️ 클립을 섞으므로 화면의 랭킹 번호가 등장 순서와 다르게 보이고, 내레이션(오디오)도 그 순서로 재배치됨(중복 회피 목적).
- 최소 세그먼트 길이(≈1.5초) 보장. 단일 장면/컷 부족 시 셔플하지 않고 원본 유지.
- **저장 위치: 셔플 결과는 `ranking_shorts/`가 아니라 `ranking_shuffled/` 폴더에 저장한다.**

### 왜 A와 다른가
- 헤더가 흰색이 아니라 검정 + 2색이라 A의 흰헤더 로직이 안 맞음.
- 자막이 흰 밴드가 아니라 영상에 박혀 있어(동적) 자동 교체가 어려움 → 자막은 손대지 않음.
- 반전 시 베이크된 텍스트가 깨져 → 반전 대신 **하단 배너**로 차별화.

---

## 파일명 규칙

변환 결과 파일은 **원본 파일명이 아니라 `<제목>.mp4`** 로 저장한다.

- **제목 출처**
  - 자막형(해숏티): 원본 파일명에서 날짜접두(`YYYYMMDD_`)·`[영상ID]`를 제거한 제목.
  - 랭킹형(랭킹굳): OCR로 읽은 제목 = `설명` + " " + `랭킹 TOPn` (예: `역대급 완벽한 박수 랭킹 TOP5`).
- **정리(sanitize)**: 파일 시스템 금지문자(`/ \ : * ? " < > |` 등)는 제거/치환, 앞뒤 공백 제거,
  과도한 길이는 잘라냄(예: 100자).
- **충돌 방지**: 같은 제목이 이미 있으면 뒤에 `_2`, `_3` … 또는 `[영상ID]`를 붙여 겹치지 않게 한다.
- 예: `20260616_남편이 남긴 마지막 소원 [xi2jY2XSf3U].mp4` → **`남편이 남긴 마지막 소원.mp4`**

> ⚠️ 주의: 자동 업로드는 `output_shorts/.yt-upload-state.json`을 **파일명 기준**으로 중복 판단한다.
> 이미 업로드된 영상의 파일명을 바꾸면 "신규"로 인식돼 재업로드될 수 있으니,
> 이 규칙은 **새로 변환하는 영상부터** 적용한다.

---

## 다운로드 규칙 (`down.js`)

- 채널 쇼츠 일괄 다운로드. **H.264(avc1) + AAC 우선** → QuickTime/모든 곳 호환(AV1/VP9 회피).
- `--merge-output-format mp4`, `--download-archive`(중복방지·이어받기), 요청 간격 2~5초.
- 사용:
  ```bash
  node down.js @채널핸들                         # 핸들의 쇼츠
  node down.js "https://youtube.com/channel/UC.../shorts" ~/Downloads/<폴더>
  node down.js @채널핸들 --videos                # 일반 동영상 탭
  ```

## 업로드 규칙 (`scripts/youtube-upload.mjs` + launchd)

- `output_shorts/`의 영상을 **비공개 업로드 + `publishAt` 예약** → **매일 18:00(KST) 1개씩 자동 공개**.
- `output_shorts/.yt-upload-state.json`로 중복 업로드 방지·이어하기.
- 일일 API 할당량 ~10,000 → 업로드 1건 1600 units → **하루 약 6개**까지.
- launchd `com.haesyotti.ytupload` 매일 11:00 자동 실행.

---

## 실행 명령 요약

```bash
# ── 자막형(해숏티) ──
python3 .shorts_work/prep.py "<video.mp4>"          # 경계+OCR+제목 → data/<id>.json
python3 .shorts_work/detect2.py data/<id>.json --apply   # 경계 재감지(선택)
# (자막 세그먼트 new 필드 = 패러프레이즈 채우기)
python3 .shorts_work/render.py data/<id>.json       # 헤더+자막교체+좌우반전 → output_shorts/
python3 .shorts_work/voice_swap.py output_shorts/*.mp4   # (선택) 배경음악 제거

# ── 랭킹형(랭킹굳) ──
python3 .shorts_work/rank_pipeline.py ranking_original              # 폴더 일괄 → ranking_shorts/
python3 .shorts_work/rank_pipeline.py --shuffle "<video.mp4>"       # 클립 순서 셔플 적용
# 결과 파일명은 <제목>.mp4 (예: 역대급 신나는 군대 생활 랭킹 TOP6.mp4)

# ── 다운로드 ──
node down.js "<채널 shorts URL>" ~/Downloads/<폴더>
```

## 규칙 요약 한 줄

- **자막형**: 흰 헤더→해숏티 / 하단자막→패러프레이즈 / 영상 좌우반전 / (선택)BGM 제거.
- **랭킹형**: 검정 헤더 제목만 재스타일(노랑+흰, 로고 없음) / 하단 랭킹굳 배너 / 반전·자막 없음.
- 공통: 원본 대비 **브랜딩 교체 + 텍스트/화면 변형으로 중복 회피**, H.264 mp4 출력.
- 파일명: 결과물은 **`<제목>.mp4`** (원본 파일명 아님).
